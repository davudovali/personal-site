import {Button} from "../../components/Button/Button";

export const ComponentsPreview = () => {
  return <div style={{display: 'flex', padding: '2rem', justifyContent: 'flex-start', alignContent: 'flex-start', gap: '1rem'}}>
    <Button label='My button'/>
  </div>
}